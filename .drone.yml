---
kind: 'pipeline'
name: 'default'
steps:
  - name: 'Lint'
    image: 'quay.io/ansible/molecule'
    commands:
      - 'molecule lint'
      - 'molecule syntax'

  - name: 'Test'
    image: 'quay.io/ansible/molecule'
    commands:
      - 'molecule cleanup'
      - 'molecule destroy'
      - 'molecule converge'
      - 'molecule idempotence'
      - 'molecule verify'
    privileged: True
    volumes:
      - name: 'docker'
        path: '/var/run/docker.sock'

  - name: 'Cleanup'
    image: 'quay.io/ansible/molecule'
    commands:
      - 'molecule cleanup'
      - 'molecule destroy'
    privileged: True
    volumes:
      - name: 'docker'
        path: '/var/run/docker.sock'

volumes:
  - name: 'docker'
    host:
      path: '/var/run/docker.sock'

