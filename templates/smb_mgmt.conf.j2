# {{ ansible_managed }}

[global]
  realm = {{ adjoin_mgmt_domain_name|upper }}
  workgroup = {{ adjoin_mgmt_workgroup_name }}
  security = ADS
  client signing = yes
  client use spnego = yes
  kerberos method = secrets and keytab
  log file = /var/log/samba/%m.log
