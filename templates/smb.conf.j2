# {{ ansible_managed }}

[global]
  realm = {{ adjoin_domain_name|upper }}
  workgroup = {{ adjoin_workgroup_name }}
  security = ADS
  client signing = yes
  client use spnego = yes
  kerberos method = secrets and keytab
  log file = /var/log/samba/%m.log
